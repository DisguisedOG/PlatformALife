For quick agent onboarding, see the repository-level `AGENTS.md`.

This file points to the canonical agent guide: [AGENTS.md](AGENTS.md)

Checklist for agents:
- Run `dotnet build` before running a Mono Godot session.
- If you change atlas or tileset resources, run `build_tileset.gd` and clear `.godot/imported/`.
